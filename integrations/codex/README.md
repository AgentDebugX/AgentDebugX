# Codex plugin

Native Codex marketplace bundle for project-scoped AgentDebugX capture and
explicit trajectory diagnosis. It uses the installed `agentdebug` CLI; the
legacy direct-hook CLI remains supported.
