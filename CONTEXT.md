# AgentDebugX Evaluation

Vocabulary for experiments that compare AgentDebugX-assisted recovery with
unassisted behavior.

## Language

**Baseline**:
The initial Seed attempt that provides the comparison point for recovery.

**Method**:
An executable evaluation workflow, such as `control`, `deep`, `rerun`, or
`rerun-deep`. A method defines the inputs and behavior used for an attempt.
