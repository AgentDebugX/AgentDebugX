# Capture Hosts

Host adapters translate supported Claude Code and Codex JSONL records into the
shared capture model. Keep host-specific schemas and event mappings here;
storage, filtering, and capture orchestration belong in the parent package.

Codex automatic capture supports CLI versions `>=0.145.0`. See
[Codex support](CODEX_SUPPORT.md) for the compatibility contract and source
audit.
